{
  "name": "dsh-llm-claude",
  "version": "0.3.0",
  "type": "module",
  "description": "Claude Code CLI as an LLM provider for dsh (DeepSeek Harness): live model list, session resume, images, tool activity",
  "main": "src/adapter.js",
  "exports": {
    ".": "./src/adapter.js"
  },
  "scripts": {
    "test": "node src/adapter.test.js",
    "check": "oxlint src/ --max-warnings=0 && oxfmt --check src/ && node src/adapter.test.js",
    "lint": "oxlint src/ --max-warnings=0",
    "format": "oxfmt src/",
    "deadcode": "fallow"
  },
  "dependencies": {},
  "devDependencies": {
    "fallow": "^3.17.0",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.80.0"
  },
  "peerDependencies": {
    "@deepseek-ai/dsh-llm": "^0.1.2-alpha.4",
    "@deepseek-ai/cordis": "^4.0.2",
    "@deepseek-ai/schemastery": "^3.18.0"
  },
  "files": [
    "src",
    "README.md"
  ]
}
