node_modules
.cache/
# agent worktrees live here, never tracked
.claude/worktrees/
.serena/

# dsh drops pasted files here per session
.dsh-filess/

# owner working notes, not part of the repo: agent instructions, agent state,
# the ranked queue and the surveys behind it. Anything a reader may need goes
# under docs/, which is tracked whole; anything private goes under notes/.
CLAUDE.md
.claude/
notes/
