node_modules
.cache/
# agent worktrees live here, never tracked
.claude/worktrees/
.serena/

# dsh drops pasted files here per session
.dsh-filess/

# owner working notes, not part of the repo: agent instructions, agent state,
# the ranked queue and the surveys behind it. README images stay tracked.
CLAUDE.md
.claude/
docs/*
!docs/media/

# plans and worker briefs are tracked: they are the record of how a batch was built
!docs/plans/
